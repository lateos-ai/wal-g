name: Compatibility Test
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  compatibility:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-go@v6
      with:
        go-version: '1.25'
    - name: Run compatibility tests
      run: echo "Compatibility test placeholder"
